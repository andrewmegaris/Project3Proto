<%= form_for(@folder) do |f| %>
  <% if @folder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@folder.errors.count, "error") %> prohibited this folder from being saved:</h2>

      <ul>
      <% @folder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.collection_select :user_id, User.all, :id, :name %> <br>
  </div>
  
  <div class="field">
    <%= f.collection_select :parent_id, Folder.all, :id, :name %> <br>
  </div>
  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
